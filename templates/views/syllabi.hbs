{{!< default}}
{{!-- 
/**
 * Boston Civic Media Website
 * Developed by Engagement Lab, 2016
 * ==============
 * 
 * Syllabi Directory Page Template 
 * - Use {{jsonPrint project}} to print entire json...DEBUG ONLY
 * ==========
 */
--}}

<!-- DIRECTORY START -->
<!-- ================ -->
<section class="section main-container padding-bottom-clear">
	<div class="container-fluid">
		<div class="row main-syllabi">
			<h1 class="col-md-12 syllabi-title page-title"><strong>Syllabi</strong></h1>
			{{!-- {{> page/lead}} --}}
		</div>
	</div>

	{{!-- We'll need some filtering dropdowns here --}}


	{{!-- Show all syllabi below by default --}}
	<div class="container pv-20">

		<div class="row">
			<div class="col-md-12 filters">
			  {{#each filters}}

				<div class="btn-group clearfix" >
					<button id="syllabi{{label}}" class="syllabi-options btn btn-default dropdown-toggle" type="button" data-filter-group="{{label}}" data-toggle="dropdown">{{label}} <span class="bigCaret"></span></button>
					<ul class="dropdown-menu scrollable" role="menu">
						<li><a data-filter=""  href="#" data-filter-value="" data-filter-group="{{label}}" class="option-{{label}}">All {{label}}s</a></li>

					    {{#each values}}
						    <li><a value="#" href="#" data-filter-value=".{{key}}" data-filter-group="{{../label}}" class="option-{{../label}}">{{name}}</a></li>
					    {{/each}}
					</ul>
				</div>

			  {{/each}}
			</div>
		</div>

		<div class="no-syllabi-visible">
			<h2>No Syllabi match those categories!</h2>
		</div>

		<div id="syllabi-content" class="syllabi row">
			
			{{#if syllabi}}

				{{!-- Show syllabi --}}
				{{#each syllabi}}


					<div class="col-md-6 syllabi-item {{filters}}">
						<div class="syllabus">
							<a href="/syllabi/{{syllabus_key}}"><h3 class="styled-title">{{title}}</h3></a>
							<p>{{{description}}}</p>
							{{!-- <p>{{jsonPrint this}}</p> --}}
							<div class="row tags">

							{{#if institution}}
								<div class="col-md-4">
									<p><strong>Institution (s)</strong></p>
									<ul>
										{{#each institution}} <li>- {{this.name}}</li> {{/each}}
										
									</ul>
								</div>
							{{/if}}

							{{#if discipline}}
								<div class="col-md-4">
									<p><strong>Discipline (s)</strong></p>
									<ul>
										{{#each discipline}} <li>- {{this.name}}</li> {{/each}}
										
									</ul>
								</div>
							{{/if}}

							{{#if faculty}}
								<div class="col-md-4">
									<p><strong>Faculty Member (s)</strong></p>
									<ul>
										{{#each faculty}} <li>- {{this.name}}</li> {{/each}}
										
									</ul>
								</div>
							{{/if}}

								{{!-- <li>{{#each discipline}} {{this.name}} {{/each}}</li>
									<li>{{#each faculty}} {{this.name}} {{/each}}</li>
									{{#if keyword}}<li>{{#each keyword}} {{this.name}} {{/each}}</li>{{/if}}
									{{#if partnerOrg}}<li>{{#each partnerOrg}} {{this.name}} {{/each}}</li>{{/if}} --}}
							</div>
						</div>
					</div>

				{{/each}}

			{{else}}
				
				<strong>No syllabi! Fiddlesticks!</strong>
			
			{{/if}}
		</div>
	</div>
</section>
