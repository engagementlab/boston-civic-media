{{!-- 
/**
	* Boston Civic Media
	* Developed by Engagement Lab, 2016
	* ==============
*/
--}}

<div class="feature-wrap col-md-4 col-sm-4 col-xs-12">
{{#if url}}
	<a href="{{url}}">
	<div class="feature link">
		{{else}}
		<div class="feature">
		{{/if}}
		<h1>{{name}}</h1>
		<h2>{{description}}</h2>
		{{#if url}}
		<div class="bottom"><span>Learn More</span></div>
	</div></a>
	{{else}}
	</div>
{{/if}}
</div>